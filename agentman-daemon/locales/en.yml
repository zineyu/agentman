_version: 2

# Daemon startup/shutdown
daemon:
  starting: "Agentman Daemon starting..."
  runtime_id: "Runtime ID: {id}"
  base_url: "Base URL: {url}"
  init_table_ids: "Initializing table IDs..."
  check_existing_runtime: "Checking for existing runtime..."
  found_existing_runtime: "Found existing runtime {id} for hostname {hostname}, reusing"
  no_existing_runtime: "No existing runtime found, registering new runtime..."
  once_mode: "Running in once mode"
  start_main_loop: "Starting main loop"
  received_shutdown_signal: "Received shutdown signal, stopping executor..."
  executor_shutdown_error: "Error waiting for executor to shutdown: {error}"
  failed_update_status: "Failed to update runtime status to offline: {error}"
  shutting_down: "Agentman Daemon shutting down..."

# Base client
base_client:
  token_refreshed: "Access token refreshed, expires in {seconds} seconds"
  table_ids_initialized: "Table IDs initialized: task={task}, runtime={runtime}, log={log}"
  tasks_table_not_found: "Tasks table not found in Base"
  api_request: "API request: {method} {path} (attempt {attempt})"
  parse_response_error: "Failed to parse response as JSON (status: {status}): {error}\nRaw response: {raw}"
  rate_limited: "HTTP 429 rate limited, retrying in {delay:?}"
  unauthorized: "HTTP 401 unauthorized, clearing token cache and retrying"
  retryable_error: "Retryable API error {status} ({message}), retrying in {delay:?}"
  network_error: "Network error (attempt {attempt}): {error}, retrying in {delay:?}"
  fetch_pending_tasks: "Fetching pending tasks"
  fetched_pending_tasks: "Fetched {count} pending tasks for runtime {runtime}"
  update_task_status: "Updating task status and appending execution log"
  updated_task_status: "Updated task {id} status to {status}"
  clear_rejection_reason: "Clearing task rejection reason"
  cleared_rejection_reason: "Cleared rejection reason for task {id}"
  fetch_task_fields: "Fetching specified fields for task"
  find_runtime_by_hostname: "Finding existing runtime by hostname"
  found_existing_runtime: "Found existing runtime {id} for hostname {hostname}"
  register_runtime: "Registering runtime"
  registered_runtime: "Registered runtime {id}"
  update_heartbeat: "Updating runtime heartbeat"
  runtime_not_found_registering: "Runtime {id} not found, registering instead"
  updated_heartbeat: "Updated heartbeat for runtime {id}"
  create_execution_log: "Creating execution log, returning record ID"
  created_execution_log: "Created execution log {id} for task sequence {seq}"
  update_execution_log: "Updating execution log"
  updated_execution_log: "Updated execution log {id}"

# Task executor
task_executor:
  executing_once_mode: "Executing tasks (once mode)"
  start_execution_loop: "Starting task execution loop"
  shutdown_requested: "Shutdown requested, stopping execution loop"
  error_processing_tasks: "Error processing tasks: {error}"
  found_pending_tasks: "Found {count} pending tasks"
  processing_task: "Processing task {id}: {name}"
  empty_record_id: "Task {id} has empty record_id, skipping"
  retrying_after_rejection: "Task {id} is retrying after rejection: {reason}"
  failed_clear_rejection: "Failed to clear rejection reason: {error}"
  failed_update_status_in_progress: "Failed to update task status to in-progress: {error}"
  failed_create_execution_log: "Failed to create execution log: {error}"
  failed_create_adapter: "Failed to create agent adapter: {error}"
  adapter_creation_failed: "Agent adapter creation failed: {error}"
  agent_execution_error: "Agent execution error: {error}"
  failed_flush_log: "Failed to flush execution log: {error}"
  task_completed_success: "Task {id} completed successfully, status set to pending-review"
  task_failed_retry: "Task {id} failed, retry count: {current}/{max}"
  failed_update_execution_log: "Failed to update execution log: {error}"
  failed_update_status_pending_review: "Failed to update task status to pending-review: {error}"
  failed_update_status_failure: "Failed to update task status on failure: {error}"
  task_status_updated_after_failure: "Task {id} status updated to {status} after failure"

# Heartbeat
heartbeat:
  sent_successfully: "Heartbeat sent successfully"
  failed: "Failed to send heartbeat: {error}"

# CLI adapter
cli_adapter:
  cannot_auto_detect: "Cannot auto-detect CLI for Other agent type"
  no_cli_found: "No CLI tool found for {agent_type}. Tried: {tried:?}"
  executing_task: "Executing task {id} with {cli} CLI in workspace {workspace}"
  running_command: "Running command: {command:?}"
  failed_execute: "Failed to execute {cli}: {error}"
  process_execution_failed: "Process execution failed: {error}"
  task_timeout: "Task {id} execution timed out after 30 minutes"
  task_executed_success: "Task {id} executed successfully with {cli}"
  task_failed_exit_code: "Task {id} failed with exit code {code} using {cli}"
  process_exited: "Process exited with code {code}"
  executing_task_streaming: "Executing task {id} with {cli} CLI in workspace {workspace} (streaming)"
  failed_spawn: "Failed to spawn {cli}: {error}"
  process_spawn_failed: "Process spawn failed: {error}"
  failed_wait: "Failed to wait for {cli}: {error}"
  process_wait_failed: "Process wait failed: {error}"

# Workspace
workspace:
  created: "Created workspace at {path:?}"
  already_exists: "Workspace already exists at {path:?}"
  cleaned_up: "Cleaned up workspace at {path:?}"
  does_not_exist: "Workspace does not exist, skipping cleanup: {path:?}"
